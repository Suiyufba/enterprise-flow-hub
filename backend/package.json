{
  "name": "backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsc -p tsconfig.json && mkdir -p dist/db && cp src/db/schema.sql src/db/seed.sql dist/db/",
    "start": "node dist/main.js",
    "lint": "echo \"No backend lint configured yet\"",
    "test": "echo \"No backend tests yet\""
  },
  "dependencies": {
    "@fastify/cors": "^11.0.1",
    "better-sqlite3": "^12.10.0",
    "fastify": "^5.3.3",
    "shared": "workspace:*",
    "zod": "^3.25.28"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^22.15.21",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3"
  }
}
